{% extends 'base.html.twig' %}

{% block title %}New Utilisateur{% endblock %}

{% block body %}
    <section class="p-3 p-md-5">
        <h1><i class="fas fa-user-plus"></i> Nouvel utilisateur<span
                    class="text-danger">.</span></h1>
        <div class="rounded overflow-hidden shadow-lg bg-white px-3 pb-1">
            <div class="py-3">{{ include('utilisateur/_form.html.twig') }}</div>
            <footer class="row bg-light p-3 rounded">
                <div class="col">
                    <a href="{{ path('utilisateur_index') }}" class="btn btn-dark"><i class="fas fa-backward"></i></a>
                </div>
                <div class="col text-end">
                    <button class="btn btn-success" form="form_utilisateur" type="submit"><i class="fas fa-check"></i>
                        Créer l'utilisateur
                    </button>
                </div>
            </footer>
        </div>
    </section>
{% endblock %}
